# mybm開発方針

## 説明エディタ

- Cosenseの操作仕様を基準にする
- カーソルがある行はリンクのレンダリングを解除し、`[]` の記法を表示する
- 箇条書きと階層表示は選択状態にかかわらず常に表示する
- カーソルがない行は行頭空白を箇条書き階層として表示する
- 箇条書き記号はCSS疑似要素ではなく実DOM要素として描画する
- カーソルがない行の `[]` はリンクとして表示する
- 編集中の正本はCodeMirrorのドキュメントとする
- 保存時はCodeMirrorのドキュメントをプレーンテキストとして保存する
- 箇条書きとリンクはCodeMirrorのDecorationで同一ドキュメント上に描画する
- 編集中に入力欄と別の描画層を同期しない
- 入力のたびにエディタDOMを再構築しない
- compositionイベントでは入力値を変更しない
- ドキュメント変更では自動保存とLinksの更新だけを行う
- Enterで不要なインデントを自動挿入しない
- Tabはインデントとして1文字だけ挿入する
