{
  "name": "mybm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild popup.js --bundle --format=esm --outfile=dist/popup.js",
    "test": "node --test"
  },
  "devDependencies": {
    "@codemirror/state": "^6.7.4",
    "@codemirror/view": "^6.43.11",
    "esbuild": "^0.28.2"
  }
}
